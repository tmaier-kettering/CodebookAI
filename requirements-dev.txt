# Development / testing dependencies
# Install with: pip install -r requirements-dev.txt

pytest>=7.4.0          # Test runner
pytest-mock>=3.12.0    # Convenient mocker fixture (wraps unittest.mock)
